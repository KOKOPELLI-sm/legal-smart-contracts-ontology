[project]
name = "legal-smart-contracts-ontology"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "slither-analyzer>=0.11.3",
]
